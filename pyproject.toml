[project]
name = "urdf-ops"
version = "0.1.0"
description = "Training operations UI and backend for URDF Studio"
requires-python = ">=3.10"
dependencies = [
  "fastapi",
  "uvicorn",
  "pydantic",
  "huggingface_hub",
  "torch",
  "lerobot",
  "safetensors",
]

[project.optional-dependencies]
dev = ["pytest"]
